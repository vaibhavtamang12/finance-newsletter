# RSS Parsing
feedparser>=6.0.11

# HTTP Requests
requests>=2.31.0

# Environment Variables
python-dotenv>=1.0.1

# Groq AI SDK (free tier, high RPM)
groq>=0.9.0

# Template Engine
Jinja2>=3.1.4

# Live market data (no API key required)
yfinance>=0.2.40

# Inlines CSS into HTML tags so the newsletter renders correctly in Gmail,
# Outlook, etc. (most email clients strip external <link> stylesheets and
# ignore or mangle <style> blocks)
premailer>=3.10.0